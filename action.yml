name: 'Composer (php-actions)'
description: 'Use the Composer CLI in your Github Actions.'
inputs:
  command:
    description: 'Composer command to run'
    required: true
    default: install --working-dir=plugins/ups-algolia --no-interaction

outputs:
  time:
    description: 'The time the action was run'
runs:
  using: 'docker'
  image: 'Dockerfile'
  args:
    - install --working-dir=plugins/ups-algolia --no-interaction
    - --no-progress
branding:
  icon: 'package'
  color: 'purple'
